<tr>
  <td><%= city.id %></td>
  <td><%= city.name %></td>
  <td><%= link_to 'Редактировать', edit_city_path(city.id), class: "btn btn-outline-primary" if current_employee.allowed_to?(:edit, 'cities') %></td>
</tr>